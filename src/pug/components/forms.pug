mixin formEstimate(nameClass = false, link = 'privacy-policy.html')
  form(class= nameClass, class="form-estimate", action="", method="post", enctype="multipart/form-data")
    .form-estimate__row
      .form-estimate__col
        input(type="tel", placeholder="+7 (___) ___-__-__ *", required)
      .form-estimate__col
        input(type="text", maxlength="50", placeholder="Марка и модель авто")

    .form-estimate__row.form-estimate__row--full
      textarea(rows="5", cols="67", placeholder="Описание проблемного места автомобиля")

    .form-estimate__row.form-estimate__row--full
      +formInputFile('form-estimate__upload')

    .form-estimate__row.form-estimate__row--bottom
      .form-estimate__col
        input.btn(type="submit", value="Отправить на оценку")
      .form-estimate__col
        p.form-estimate__agreement
          | Нажимая на кнопку «Отправить на оценку», Вы соглашаетесь с !{' '}
          a(href= link, target="_blank") условиями обработки персональных данных
          | .

mixin formGetAdvice()
  p Test

mixin formInputFile(nameClass = '', valueAccept = 'image/jpeg,image/png,video/*')
  div(class= nameClass, class="upload")
    .upload__row
      input.upload__input(type="file", name="photos", multiple, hidden="hidden")
      button.upload__btn(type="button") Прикрепить
        span.upload__btn-mobile #{' '}фото
        span.upload__btn-desktop #{' '}фотографии
        span.upload__icon
          +icon-mono('camera')(width="20px", height="20px")
      span.upload__text Файл не выбран...
    .upload__row
      ul.upload__list
    .upload__row
      .upload__notice-error

